server:
  port: 8080

spring:
  application:
    name: spring

  datasource:
    url: jdbc:postgresql://localhost:5432/insurance_db
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true