server:
  port: 8081

spring:
  application:
    name: loan-service
  datasource:
    url: jdbc:mysql://localhost:3306/loan_management_system
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true