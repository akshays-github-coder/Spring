spring:
  application:
    name: spring-security-6
  datasource:
    url: jdbc:mysql://localhost:3306/users
    username: root
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update